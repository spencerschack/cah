import { breakNewlines } from 'cards-against-humanity/helpers/break-newlines';
import { module, test } from 'qunit';

module('Unit | Helper | break newlines');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = breakNewlines([42]);
  assert.ok(result);
});

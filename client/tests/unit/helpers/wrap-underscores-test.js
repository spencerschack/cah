import { wrapUnderscores } from '../../../helpers/wrap-underscores';
import { module, test } from 'qunit';

module('Unit | Helper | wrap underscores');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wrapUnderscores(42);
  assert.ok(result);
});
